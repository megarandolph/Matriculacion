@model IEnumerable<Matriculacion.Models.Imparte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Materia.Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profesor.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Materia.Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImparteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImparteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImparteId })
        </td>
    </tr>
}

</table>
